# Changesets App

App for reviewing and approving changesets within Safe Wallet.

# Development

Start the app:

```sh
npm start
```

Then go to https://app.safe.global -> Apps -> My Custom Apps -> Add Custom Safe App. Enter url `http://localhost:3000` and add.
